# config.yaml

# path or URL to experiment sheet (TSV format)
experiments: config/experiments.tsv

# define directories
out_dir: results # default value

# define parameters for a specific rule
basecalling: False

# path to reference files
reference: resources/ref.fa

# parameters for computational resources
threads: 4
memory: 16G
